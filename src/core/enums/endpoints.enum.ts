export enum Endpoints {
  Auth = 'auth',
  Cinemas = 'cinemas',
  Genres = 'genres',
  Movies = 'movies',
  Screenings = 'screenings',
  Seats = 'seats',
  Tickets = 'tickets',
  UserRoles = 'userroles',
  Users = 'users',
}
